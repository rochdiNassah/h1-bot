#!/usr/local/bin/php
<?php declare(strict_types=1);

$root = substr(__FILE__, 0, -strlen(basename(__FILE__)));

require $root.'/vendor/autoload.php';

use Automation\Core\Facades\Console;

$app = Automation\Core\Application::instance();

$app->run();

Console::run();